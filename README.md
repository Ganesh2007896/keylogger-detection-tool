# keylogger-detection-tool
The Keylogger Detection Tool scans all running processes in the system using the psutil library. It checks each process name against a list of suspicious keywords like “keylog” or “spy.” If a match is found, it alerts the user by displaying the process name and PID. This helps identify potential keylogger programs running in the background.
